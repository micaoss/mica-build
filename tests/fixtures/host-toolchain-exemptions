# Registered host-toolchain exceptions: path<TAB>tool<TAB>reason.
# Stale rows fail the host-toolchain gate.
tests/gates/trust-stage-test.sh	openssl	a throwaway self-signed certificate pair for the trust staging test; no producer output, nothing shipped
tests/gates/package-gate.test.ts	dpkg-deb	fixture archives for the package gate's own test, packed by the dpkg-deb this host and the tools image carry; no producer output, nothing shipped
